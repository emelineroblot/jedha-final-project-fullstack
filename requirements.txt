# ============================================================
# Dépendances de l'APPLICATION — c'est ce fichier que lit
# Streamlit Community Cloud au déploiement.
#
# Volontairement minimal : y laisser MLFlow, XGBoost ou JupyterLab
# alourdit le build de plusieurs minutes pour des paquets que
# l'application n'importe jamais.
#
# Pour développer (ETL, entraînement, notebooks, tests) :
#     pip install -r requirements-dev.txt
#
# scikit-learn est ÉPINGLÉ à la version qui a sérialisé les modèles :
# désérialiser un estimateur avec une autre version produit au mieux
# un avertissement, au pire un modèle silencieusement incorrect.
# ============================================================

streamlit==1.55.0
pandas==2.3.1
numpy==2.3.1
plotly==6.3.0
scikit-learn==1.7.2
joblib==1.5.2
sqlalchemy==2.0.41
psycopg2-binary==2.9.10
python-dotenv==1.0.1
